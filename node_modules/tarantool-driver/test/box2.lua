#!/usr/bin/env tarantool
box.cfg{listen=33015}

if not box.space.tags then
local s = box.schema.space.create('tags')
s:create_index('primary', {type = 'TREE', unique = true, parts = {1, 'string'}})
end

box.schema.user.grant('guest', 'read,write,execute', 'universe')
